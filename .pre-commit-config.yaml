repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.15.5
  hooks:
    # Run the linter.
    - id: ruff-check
      exclude: ^(migrations/|alembic\.ini).*
    # Run the formatter.
    - id: ruff-format
      exclude: ^(migrations/|alembic\.ini).*
